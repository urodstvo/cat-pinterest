import { useTitle } from '@/shared/hooks';

export const HomePage = () => {
    useTitle('Cat Pinterest');

    return (
        <>
            <header></header>
            <main></main>
        </>
    );
};
